<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= locals?.appName?locals?.appName:'404 Not Found' %> </title>
  <link rel="shortcut icon" href="/img/icon.png" type="image/x-icon">
</head>

<body>
  <style>
    /* Main styles */
    @import url(https://fonts.googleapis.com/css?family=Open+Sans:800);

    .text {
      fill: none;
      stroke-width: 4;
      stroke-linejoin: round;
      stroke-dasharray: 70 330;
      stroke-dashoffset: 0;
      -webkit-animation: stroke 6s infinite linear;
      animation: stroke 6s infinite linear;
    }

    .text:nth-child(5n + 1) {
      stroke: #F2385A;
      -webkit-animation-delay: -1.2s;
      animation-delay: -1.2s;
    }

    .text:nth-child(5n + 2) {
      stroke: #F5A503;
      -webkit-animation-delay: -2.4s;
      animation-delay: -2.4s;
    }

    .text:nth-child(5n + 3) {
      stroke: #8aed11;
      -webkit-animation-delay: -3.6s;
      animation-delay: -3.6s;
    }

    .text:nth-child(5n + 4) {
      stroke: #56D9CD;
      -webkit-animation-delay: -4.8s;
      animation-delay: -4.8s;
    }

    .text:nth-child(5n + 5) {
      stroke: #3AA1BF;
      -webkit-animation-delay: -6s;
      animation-delay: -6s;
    }

    @-webkit-keyframes stroke {
      100% {
        stroke-dashoffset: -400;
      }
    }

    @keyframes stroke {
      100% {
        stroke-dashoffset: -400;
      }
    }

    /* Other styles */
    html,
    body {
      height: 100%;
    }

    body {
      background: rgb(248, 248, 248);
      background-size: .2em 100%;
      margin: 0;
      font-family: open Sans, Impact;
    }

    .container {
      position: absolute;
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
    }

    .svg-cont {
      max-width: 600px;
      width: inherit;
      min-width: 100px;
      font: 14.5em/1 Open Sans, Impact;
      text-transform: uppercase;
      padding: 0;
      margin: 0;
    }

    .message {
      margin: 0;
      padding: 20px;
    }
  </style>

<h1><%= message %></h1>
<h2><%= error.status %></h2>
<pre><%= error.stack %></pre>
  <div class="container">
    <div class="svg-cont">
      <svg viewBox="0 0 600 300">
        <!-- Symbol -->
        <symbol id="s-text">
          <text text-anchor="middle" x="50%" y="50%" dy=".35em">
            <%if(locals.code){%><%=code%><%}else{%>404<%}%>
          </text>
        </symbol>
        <!-- Duplicate symbols -->
        <use xlink:href="#s-text" class="text"></use>
        <use xlink:href="#s-text" class="text"></use>
        <use xlink:href="#s-text" class="text"></use>
        <use xlink:href="#s-text" class="text"></use>
        <use xlink:href="#s-text" class="text"></use>
      </svg>
    </div>
    <div class="message">
      <h2>
        <%if(locals.message){%>
        <%=message%>
        <%}else{%>
        Page Not Found
        <%}%>
      </h2>
      <p><%if(locals.error){%><%=error%><%}else{%>It looks like you found a glitch in the matrix...<%}%></p>
      <a class="btn btn-primary mt-4 text-white" href="<%if(locals.action){%><%=action%><%}else{%>/<%}%>">&larr; <%if(locals.action_message){%><%=action_message%><%}else{%>Back to Home<%}%></a>
    </div>
  </div>

</body>

</html>